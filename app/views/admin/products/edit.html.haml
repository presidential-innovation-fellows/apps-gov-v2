%section.edit-product.usa-grid
  %header
    %h1
      = t(".header")
    = simple_form_for [:admin, @product] do |f|
      = render "product_table", product_draft: @product_draft

      = f.submit t(".submit")
