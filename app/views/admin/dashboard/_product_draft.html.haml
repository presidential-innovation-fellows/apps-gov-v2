= content_tag_for :tr, product_draft do
  %td
    = link_to product_draft.item.name,
      edit_admin_product_path(product_draft.item)
  %td
    = product_draft.created_at.strftime("%m/%d/%y")
  %td
    = User.find(product_draft.whodunnit).name
